name: Set Fly secrets

# One-shot workflow that pushes the LITESTREAM_* values from GitHub Actions
# secrets into Fly machine secrets. Uses --detach so the GH job doesn't
# block on the machine restart triggered by the secret update.
#
# Re-run after rotating R2 credentials or after the initial bucket setup.
# Required GH secrets:
#   LITESTREAM_BUCKET, LITESTREAM_ENDPOINT,
#   LITESTREAM_ACCESS_KEY_ID, LITESTREAM_SECRET_ACCESS_KEY

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  set-secrets:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1

      - name: Push LITESTREAM_* secrets to Fly
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          LITESTREAM_BUCKET: ${{ secrets.LITESTREAM_BUCKET }}
          LITESTREAM_ENDPOINT: ${{ secrets.LITESTREAM_ENDPOINT }}
          LITESTREAM_ACCESS_KEY_ID: ${{ secrets.LITESTREAM_ACCESS_KEY_ID }}
          LITESTREAM_SECRET_ACCESS_KEY: ${{ secrets.LITESTREAM_SECRET_ACCESS_KEY }}
        run: |
          if [ -z "$LITESTREAM_BUCKET" ] || [ -z "$LITESTREAM_ENDPOINT" ] || \
             [ -z "$LITESTREAM_ACCESS_KEY_ID" ] || [ -z "$LITESTREAM_SECRET_ACCESS_KEY" ]; then
            echo "::error::One or more LITESTREAM_* GH secrets are unset. Set them via 'gh secret set' first."
            exit 1
          fi
          # --detach: don't wait for the restart to complete. The job returns
          # as soon as the secrets are written; the machine restarts in the
          # background. Verify via the fly-diag workflow or `fly logs`.
          flyctl secrets set \
            --app ltv-desking-pro-api \
            --detach \
            LITESTREAM_BUCKET="$LITESTREAM_BUCKET" \
            LITESTREAM_ENDPOINT="$LITESTREAM_ENDPOINT" \
            LITESTREAM_ACCESS_KEY_ID="$LITESTREAM_ACCESS_KEY_ID" \
            LITESTREAM_SECRET_ACCESS_KEY="$LITESTREAM_SECRET_ACCESS_KEY"
          echo "Secrets pushed. Machine restart in progress."
          echo "Run the 'Fly diagnostics' workflow in ~60s to confirm replication is live."
